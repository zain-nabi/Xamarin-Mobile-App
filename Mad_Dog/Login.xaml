<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mad_Dog.Login"
             xmlns:local="clr-namespace:Mad_Dog">

    <StackLayout VerticalOptions="Center" Padding="10">
        <Image Source="{local:ImageResource Mad_Dog.Images.wp2079218.jpg}" WidthRequest="500" HeightRequest="200"/>


        <Grid VerticalOptions="CenterAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
        </Grid>

        <StackLayout Grid.Row="0">
            <Entry Placeholder="Username" FontSize="16" x:Name="EntryUserName"></Entry>
        </StackLayout>
        <StackLayout Grid.Row="1">
            <Entry Placeholder="Password" FontSize="16" x:Name="EntryUserPassword"></Entry>
        </StackLayout>
        <StackLayout Grid.Row="2">
            <Button Text="Register" HorizontalOptions="CenterAndExpand" WidthRequest="100" TextColor="White" BackgroundColor="Green" Clicked="SignUpRedirect"></Button>
            <Button Text="Login" HorizontalOptions="CenterAndExpand" WidthRequest="100" TextColor="White" BackgroundColor="Green" Clicked="LoginRedirect"></Button>
        </StackLayout>
    </StackLayout>
</ContentPage>
